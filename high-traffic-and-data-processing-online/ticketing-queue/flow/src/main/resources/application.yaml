spring:
  application:
    name: flow

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 9010

scheduler:
  enabled: true

---

spring:
  config:
    activate:
      on-profile: test

  data:
    redis:
      host: localhost
      port: 16379

scheduler:
  enabled: false
